/*function Line({guess}) {
    const tiles = [];
    
    for (let i = 0; i < CODE_LENGTH; i++) {
        const num = guess[i];
        tiles.push(<div className="tile">{char}</div>)
    }

    return (
        <div className="line">{tiles}</div>
    )
  } */